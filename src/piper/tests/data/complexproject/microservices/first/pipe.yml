python: 3.7.4

dependencies:
  - common

requirements:
  jsonargon: 0.1.1

tasks:

  setup:
    pre:

      - task: jsonargon==0.1.1
        name: make-argonauts
        config:
          something: a
