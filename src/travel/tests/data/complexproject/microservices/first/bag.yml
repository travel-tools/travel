python: 3.7.4

dependencies:
  - common

requirements:
  - jsonargon==0.1.1

tasks:

  clean:
    pre:
      - pipertask: traveltaskexample
        name: deep-clean

  setup:
    pre:
      - pipertask: traveltaskexample
        name: make-argonauts-1
        config:
          something: a
      - pipertask: traveltaskexample
        name: make-argonauts-2
        config:
          something: g

    post:
      - pipertask: traveltaskexample
        name: make-argonauts-3
        config:
          something: a

scopes:

  notebooks:
    requirements:
      - jupyter==1.0.0

  tests:
    requirements:
      - pytest==7.1.1
